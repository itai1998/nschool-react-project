export interface LocalData {
  product_id: number;
  quantity: number;
}

export interface SelectedProduct {
  product_id: number;
  name: string;
  price: number;
  quantity: number;
  total: number;
}
